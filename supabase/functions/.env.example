# Local Edge Function secrets for `supabase functions serve`
# Copy to supabase/functions/.env (gitignored via .env.*) — never commit real values.
# Or set the same names via: supabase secrets set OPENAI_API_KEY=...

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
