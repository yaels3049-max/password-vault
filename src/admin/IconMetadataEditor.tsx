/**
 * @deprecated Phase 111 — use IconAssetEditor. Kept so imports/docs that
 * referenced the old metadata-only panel resolve; RegistryAdmin uses IconAssetEditor.
 */
export { default } from './IconAssetEditor';
